"use client";

import Sidebar from "@/components/Sidebar";

export default function UploadPage() {
  return (
    <Sidebar>
      <div style={{ padding: 24 }}>
        <h2 style={{ fontSize: 18, fontWeight: 700, color: "#333" }}>上传文件</h2>
        <p style={{ color: "#888", marginTop: 8 }}>文件上传区域 — 待接数据。</p>
      </div>
    </Sidebar>
  );
}
