"use client";

import AppLayout from "@/components/AppLayout";

export default function CategoriesPage() {
  return (
    <AppLayout activeType="categories">
      <div style={{ padding: 24 }}>
        <h2 style={{ fontSize: 18, fontWeight: 700, color: "#333" }}>分类管理</h2>
        <p style={{ color: "#888", marginTop: 8 }}>分类管理区域 — 待接数据。</p>
      </div>
    </AppLayout>
  );
}
